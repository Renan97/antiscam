import styled from "styled-components";

export const NavDiv = styled("div")({
    width: "70px",
    height: "100vh",
    backgroundColor: "blue",
  });
  export const BodyDiv = styled("div")({ width: "calc(100vw - 70px)", height: "100vh", padding: "10px" });
  export const MainDiv = styled("div")({ display: "flex" });
  export const NavHr = styled("hr")({
    margin: 0,
  });